/* Components section stub — built last (PLAN Phase 9, §20.4). */
import { renderStub } from '~/v2-site/layout';

export function render(): string {
	return renderStub({
		section: 'components',
		title: 'Components',
		blurb: 'The c-* component catalogue is built last, once the objects, variants, and component CSS exist.',
	});
}
