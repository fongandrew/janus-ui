/* Colors section stub — filled in after Composition (PLAN Phase 4, §20.3). */
import { renderStub } from '~/v2-site/layout';

export function render(): string {
	return renderStub({
		section: 'colors',
		title: 'Colors',
		blurb: 'The APCA contrast grid and color playground land here once the Composition section is complete.',
	});
}
