/* Tools sub-page stub — filled in with the tools layer (PLAN Phase 3, §20.2.3). */
import { renderStub } from '~/lib2-site/layout';

export function render(): string {
	return renderStub({
		section: 'composition',
		composition: 'tools',
		title: 'Tools',
		blurb: 'The narrow t-* tools layer is documented here once it is built in Phase 3.',
	});
}
