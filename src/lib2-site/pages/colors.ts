/* Colors section. Stub until Phase 3 (contrast grid) and Phase 9 (playground). */
import { stubPage } from '~/lib2-site/shell';

export function render(): string {
	return stubPage(
		'colors',
		'Colors',
		'The APCA contrast grid and color playground land in Phase 3 and Phase 9.',
	);
}
