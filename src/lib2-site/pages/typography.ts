/* Typography sub-page stub — populated by PLAN Phase 1 (§20.2.4). */
import { renderStub } from '~/lib2-site/layout';

export function render(): string {
	return renderStub({
		section: 'composition',
		composition: 'typography',
		title: 'Typography',
		blurb: 'The base-layer type showcase lands here in Phase 1.',
	});
}
