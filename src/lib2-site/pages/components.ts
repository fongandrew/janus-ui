/* Components section. Stub until Phase 3 (CSS gallery) and Phase 9 (Solid demos). */
import { stubPage } from '~/lib2-site/shell';

export function render(): string {
	return stubPage(
		'components',
		'Components',
		'The component gallery lands in Phase 3 (CSS-only) and Phase 9 (interactive).',
	);
}
