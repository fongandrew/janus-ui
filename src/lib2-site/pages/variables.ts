/* Variables sub-page stub — populated by PLAN Phase 1 (§20.2.1). */
import { renderStub } from '~/lib2-site/layout';

export function render(): string {
	return renderStub({
		section: 'composition',
		composition: 'variables',
		title: 'Variables',
		blurb: 'The --v-* token reference and live renders land here in Phase 1.',
	});
}
