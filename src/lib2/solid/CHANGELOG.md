# Changelog — @janus/solid

## Unreleased

- `ADDED` The Solid component wrappers (§13.7): `Alert`, `Textarea`, `Checkbox`,
  `Radio` / `RadioGroup`, `Toggle`, `SelectNative`, `Tag`, `Badge`, `Avatar`,
  `Spinner`, `Skeleton`, `Disclosure`, `Tooltip`, `Table`, `Tabs` / `TabList` /
  `Tab` / `TabPanel`, `Modal`, `Drawer`, `Popover`, `Menu` / `MenuItem`,
  `StyledSelect`, and `Password`. These join the existing core (`ariaize` /
  `attrs`, `useLabelledInput`, `LabelledInput` / `LabelledInline` /
  `LabelledInputGroup`, `Button` / `IconButton`, the `Card` family, `Input`, the
  `Form` family, and `ModalForm` / `ModalSpeedBump`), the aggregate `index`
  entry, and the internal `utils` (`mergeRefs`).
