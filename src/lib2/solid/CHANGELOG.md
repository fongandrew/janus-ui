# Changelog — solid/

## 2026-07-02

- ADDED: Initial v2 Solid layer — `ariaize`/`attrs` boundary helpers,
  `useLabelledInput` + `LabelledInput`/`LabelledInline`/`LabelledInputGroup`
  render-prop layouts, the full §13.7 component catalogue (button, card,
  alert, input, textarea, checkbox, radio, toggle, select-native, tag,
  badge, avatar, spinner, skeleton, disclosure, tooltip, table, tabs, modal,
  drawer, popover, menu, styled-select, password), form wrappers (`Form`,
  `FormGroup`, `FormError`, `SubmitButton`), modal form wrappers
  (`ModalForm`, `ModalSpeedBump`), and the client-rendered test app at
  `/v2-solid.html`.
