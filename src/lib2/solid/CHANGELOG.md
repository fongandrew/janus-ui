# Changelog — `solid`

## [Unreleased]

### ADDED

- Initial Solid wrapper layer (Phase 6). `ariaize` / `attrs` boundary helpers, the
  `useLabelledInput` hook + `LabelledInput` / `LabelledInline` / `LabelledInputGroup`
  layouts, and a thin wrapper for every component in §13.7: Button/IconButton, Card
  family, Alert, Input, Textarea, Password, Checkbox, Radio/RadioGroup, Toggle,
  SelectNative, Tag, Badge, Avatar, Spinner, Skeleton, Disclosure, Tooltip,
  Tabs family, Modal family, Drawer, Popover, Menu/MenuItem, StyledSelect. Form
  wrappers (Form, FormGroup, FormError, SubmitButton, FormContext) and the modal-form
  wrappers (ModalForm, ModalSpeedBump).
