# Changelog — css/

## 2026-07-02

- ADDED: Initial v2 CSS package — layer architecture, reset, `--v-*` token
  system (spacing, radius, color, typography, shadow, motion), and base
  element styles.
