# Changelog — `dom`

## [Unreleased]

### ADDED

- Initial framework-agnostic DOM behavior layer (Phase 5). `ca` attribute
  composition, the `data-js` behavior dispatcher + `mount()`, the form validation
  and submission engine, the toolkit and component-internal handler catalogue, the
  vanilla-JS component compositions, and the `all` (Pattern A) entry point.
