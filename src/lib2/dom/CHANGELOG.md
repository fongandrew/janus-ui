# Changelog — dom/

## 2026-07-02

- ADDED: Initial v2 DOM layer — compose-attrs (`ca`), behavior dispatch,
  mount scan, form engine (validators, submit choreography, touched-state,
  server-fed errors), behavior catalogue (roving focus, request-close,
  focus trap, restore focus, typeahead, active descendant, kb-nav flag,
  empty collapse, scroll shadows, form behaviors, modal close/speed bump,
  tabs select), and component producer compositions.
